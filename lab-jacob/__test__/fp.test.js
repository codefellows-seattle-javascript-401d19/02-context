'use strict';

const fp  = require('../lin/fp');


describe('fp.reduce', () => {
  
  test('should aggregate all the values to the accumulator and return that accumulated value', () => {
    
  });
  
});
